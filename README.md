# dotfiles
Contains my dot files (.vimrc, .bashrc, etc.)

## Usage

* Install Inconsolas font
* Set up symlinks:
  `ln -s $REPO/.vimrc ~/`
  `ln -s $REPO/.vim ~/`
  `ln -s REPO/.bashrc ~/.bash_profile`  (for OS X)

